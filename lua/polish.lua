-- Запускается последним. Чистый lua — всё что не влезло в модули выше идёт сюда.

-- Глушим deprecation-варнинги на nvim 0.12 (client.stop, tbl_add_reverse_lookup, ...).
-- Источник — плагины, которые AstroNvim v5 пинит к старым версиям намеренно
-- (совместимость). Сами по себе безвредны, работу не ломают, только шумят на старте.